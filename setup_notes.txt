#instructions for setting up folders and conda environment

##create a new qchem conda enviornment and folders
git clone https://github.com/icanswim/cosmosis.git
git clone https://github.com/icanswim/qchem.git
cd qchem
mkdir data
mkdir logs
mkdir models
conda env create -f environment.yml
conda activate qchem
jupyter lab

##manual creation of conda enviornment and folders
git clone https://github.com/icanswim/cosmosis.git
git clone https://github.com/icanswim/qchem.git
cd qchem
mkdir data
mkdir logs
mkdir models
conda create --name qchem -c conda-forge python==3.9 numpy pandas scipy
conda activate qchem
conda install pytorch==2.0.1 torchvision==0.9.0 torchaudio==0.8.0 pytorch-cuda==11.8 -c pytorch
conda install -c conda-forge jupyterlab
conda install -c conda-forge matplotlib
conda install -c conda-forge scikit-learn
conda install -c conda-forge h5py
conda install -c conda-forge git
conda install pyg==2.3 -c pyg -c conda-forge
jupyter lab

##download the source data for the dataset class
see each dataset class docstring for download and data prep instructions

